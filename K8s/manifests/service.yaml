# Service for the application
apiVersion: v1
kind: Service
metadata:
  name: resume-web-app
  labels:
    app: resume-web-app
spec:
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
  selector:
    app: resume-web-app
  type: ClusterIP
